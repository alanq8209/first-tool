# PRD：文本关键词定位与整段复制工具

## 1. 文档信息
- **产品名称**：文本关键词定位与整段复制工具（暂定）
- **版本**：v1.0
- **撰写日期**：2026-03-03
- **撰写人**：产品需求整理（AI）
- **目标读者**：产品、设计、前端、测试

---

## 2. 背景与问题
用户在阅读较长文本文件（如会议纪要、知识整理、日志、学习笔记）时，常常需要：
1. 通过关键词快速找到相关内容；
2. 将“完整的一段”信息复制出来，用于粘贴到其他文档或消息中。

现有做法通常需要手动查找后再选中复制，效率较低，且容易漏行或多选。

---

## 3. 产品目标
打造一个**简单、轻量、上手快**的小应用，实现：
- 导入/粘贴文本内容；
- 输入关键词后快速定位所有匹配段落；
- 一键复制某个匹配结果对应的“整段文本”。

### 3.1 成功指标（MVP）
- 关键词搜索响应时间（1MB 纯文本内）< 500ms；
- 用户从输入关键词到复制目标段落，平均操作 ≤ 3 步；
- 段落复制准确率（无截断、无多余段）≥ 99%。

---

## 4. 用户画像与使用场景

### 4.1 用户画像
- 轻度办公用户：需要从文档中提取信息发给同事；
- 内容整理者：从笔记中提取某主题段落；
- 技术用户：从日志文本中快速抽取异常相关段落。

### 4.2 核心场景
1. 用户打开应用并导入 `.txt` 文件；
2. 在搜索框输入关键词（例如“报错”“预算”“待办”）；
3. 应用展示匹配到的段落列表并高亮关键词；
4. 用户点击某条结果的“复制整段”；
5. 系统提示“已复制”。

---

## 5. 范围定义

### 5.1 MVP（本期必须）
- 支持文本来源：
  - 本地 `.txt` 文件导入；
  - 手动粘贴文本。
- 关键词搜索：
  - 单关键词搜索；
  - 大小写不敏感（可配置）。
- 段落识别规则：
  - 以空行（连续换行）作为段落分隔；
  - 若无空行，则以换行分行并按“连续命中行上下文”组段（简化方案：整行视作段）。
- 结果展示：
  - 列表形式展示命中段落摘要（前 120 字）；
  - 展示命中次数与段落序号。
- 一键复制：
  - 点击按钮复制完整段落到剪贴板。

### 5.2 非本期（后续迭代）
- 多关键词布尔搜索（AND/OR/NOT）；
- 正则表达式搜索；
- 多文件批量搜索；
- 命中结果导出为 Markdown/CSV；
- 云端同步与历史记录。

---

## 6. 功能需求（Functional Requirements）

### FR-01 文本输入
- 用户可通过“上传文件”选择 `.txt`；
- 用户可在文本框直接粘贴内容；
- 新文本加载后，自动清空上一次搜索结果。

**验收标准**
- 上传成功后能显示文本总字数；
- 粘贴超过 0 字即可搜索。

### FR-02 关键词搜索
- 用户输入关键词后触发搜索（回车或按钮）；
- 若关键词为空，提示“请输入关键词”；
- 返回所有包含关键词的段落。

**验收标准**
- 输入合法关键词后，结果列表正确显示；
- 搜索不到时显示“未找到匹配段落”。

### FR-03 段落定位与展示
- 按文本顺序展示命中段落；
- 每条结果展示：
  - 段落编号；
  - 摘要（截断展示）；
  - 关键词高亮。

**验收标准**
- 列表顺序与原文一致；
- 高亮仅作用于匹配关键词。

### FR-04 复制整段
- 每个结果项有“复制整段”按钮；
- 点击后将该段完整文本写入系统剪贴板；
- 成功后提示“已复制”。

**验收标准**
- 粘贴内容与原段落完全一致（含换行）；
- 复制失败时提示错误并引导重试。

### FR-05 基础可用性
- 支持清空搜索框与结果；
- 支持重新加载文本。

**验收标准**
- 清空操作后界面恢复初始状态。

---

## 7. 非功能需求（NFR）
- **性能**：1MB 文本搜索耗时 < 500ms；
- **稳定性**：常见操作（上传/搜索/复制）无崩溃；
- **易用性**：主要功能在单页面完成；
- **兼容性**：主流现代浏览器（Chrome/Edge/Safari 最新两个版本）；
- **隐私**：文本默认仅本地处理，不上传服务器（若为纯前端实现）。

---

## 8. 交互与页面草案（文字版）

### 8.1 页面结构
1. 顶部：应用标题 + 简短说明；
2. 左侧（或上方）：文本输入区（上传/粘贴）；
3. 右侧（或下方）：搜索区 + 结果列表；
4. 结果项内：段落摘要 + 高亮 + 复制按钮。

### 8.2 关键交互
- 上传文本后自动可搜索；
- Enter 触发搜索；
- 点击某结果可展开查看完整段落；
- 点击复制按钮后 toast 提示“已复制到剪贴板”。

---

## 9. 数据与规则定义

### 9.1 段落切分规则（MVP）
- 规则 A：按 `\n\n`（或多个空行）切分；
- 规则 B：切分后去除段首段尾空白字符；
- 规则 C：空段落不参与检索。

### 9.2 搜索规则（MVP）
- 默认大小写不敏感；
- 关键词进行普通字符串匹配（不使用正则）；
- 同一段内命中多次仅展示一条结果，但记录命中次数。

---

## 10. 异常与边界情况
- 文本为空：禁止搜索并提示；
- 关键词为空：禁止搜索并提示；
- 文本极大（>5MB）：提示“文本较大，可能影响性能”；
- 剪贴板权限受限：提示“复制失败，请手动复制”。

---

## 11. 埋点建议（可选）
- `upload_text`：上传文本；
- `search_keyword`：触发搜索；
- `search_no_result`：无结果；
- `copy_paragraph`：复制段落成功/失败。

用途：评估核心流程转化率（搜索→复制）。

---

## 12. 里程碑建议
- **M1（1-2 天）**：完成基础页面与文本加载；
- **M2（1-2 天）**：完成搜索、结果展示与高亮；
- **M3（1 天）**：完成复制、错误提示与自测；
- **M4（0.5 天）**：修复问题并交付。

---

## 13. 验收用例（示例）
1. 导入含 20 段文本的文件，输入存在关键词，返回正确段落数；
2. 输入不存在关键词，显示“未找到匹配段落”；
3. 点击“复制整段”后，粘贴结果与原段完全一致；
4. 粘贴纯文本后不上传文件也可完成搜索与复制；
5. 清空后再次导入新文本，旧结果不残留。

---

## 14. 开放问题（待确认）
- 段落分隔是否仅按空行，还是支持“标题分段（如 ### ）”？
- 是否需要“自动复制后跳转下一条结果”的效率模式？
- 是否需要保留最近 5 次搜索历史？

